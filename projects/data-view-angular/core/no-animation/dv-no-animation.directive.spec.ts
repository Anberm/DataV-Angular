import { DvNoAnimationDirective } from './dv-no-animation.directive';

describe('DvNoAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new DvNoAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
